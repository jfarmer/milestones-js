let num=1234;
//Number.toString(num);
console.log('This is converting the number to a string:', num.toString());
let strnum='1234';
console.log('This is 1234 as a string:', strnum);
console.log('This is 1234 as number;', parseInt(strnum));
let word='apples'
console.log('This is coverting string to array:', word.split(''));
let set=['one', '--two--', 'three']
console.log('This is multiple strings converted to one in an array:', set.join(''));
let set2=['one', 'two', 'three'];
console.log('This is multiple strings converted to one (|)in an array:', set2.join('|'));
